# Netlify Environment Variables Checklist
# Copy these to Netlify Dashboard: Site settings → Environment variables

## ✅ REQUIRED - You've already set these (Supabase)
✅ SUPABASE_URL=https://your-project.supabase.co
✅ SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
✅ SUPABASE_ANON_KEY=your_anon_key

## ⚠️ REQUIRED - Still need to set these:
□ NODE_ENV=production
□ PORT=8888

## ⚠️ REQUIRED - Redis (for job queues & caching)
□ UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
□ UPSTASH_REDIS_REST_TOKEN=your_redis_token

## 📝 OPTIONAL but RECOMMENDED (Add as needed):
□ PERPLEXITY_API_KEY=your_key (for market intelligence)
□ GEMINI_API_KEY=your_key (for AI content generation)
□ STRIPE_SECRET_KEY=sk_live_... (for payments)
□ STRIPE_WEBHOOK_SECRET=whsec_...
□ STRIPE_PUBLISHABLE_KEY=pk_live_...
□ SENTRY_DSN=https://... (for error tracking)

## After setting variables:
1. Go to Netlify Dashboard
2. Click "Trigger deploy" to rebuild with new variables
3. Monitor build logs for any errors
